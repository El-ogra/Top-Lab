<UserControl x:Class="TopLab.Presentation.Views.Patients.SimpleResultEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:TopLab.Presentation.Views.Patients"
             mc:Ignorable="d"
             FlowDirection="RightToLeft">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#F0F4F8" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding TestName}" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding TestCode}" FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </Border>
        
        <!-- Result Value -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <Label Content="قيمة النتيجة:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding ResultValue}" FontSize="16" Margin="0,4"/>
        </StackPanel>
        
        <!-- Result Flag -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <Label Content="علامة النتيجة:" FontWeight="SemiBold"/>
            <ComboBox Text="{Binding ResultFlag}" Margin="0,4"/>
        </StackPanel>
        
        <!-- Notes -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <Label Content="ملاحظات:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Notes}" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>
        </StackPanel>
        
        <!-- Error Message -->
        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="Red" FontWeight="SemiBold" />
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="مسح" Command="{Binding ClearCommand}" Margin="0,0,8,0"/>
            <Button Content="حفظ" Command="{Binding SaveCommand}" IsDefault="True"/>
        </StackPanel>
    </Grid>
</UserControl>