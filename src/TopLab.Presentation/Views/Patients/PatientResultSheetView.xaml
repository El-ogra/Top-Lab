<UserControl x:Class="TopLab.Presentation.Views.Patients.PatientResultSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:TopLab.Presentation.Views.Patients"
             mc:Ignorable="d"
             FlowDirection="RightToLeft">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Patient Header -->
        <Border Grid.Row="0" Background="#F0F4F8" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding PatientName}" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding LabId, StringFormat='الرقم المعملي: {0}'}" FontSize="14" Foreground="Gray"/>
                <TextBlock Text="{Binding TotalCount, StringFormat='عدد التحاليل: {0}'}" FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </Border>
        
        <!-- Results Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding PatientSheet.Lines}" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowHeaderWidth="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="اسم التحليل" Binding="{Binding TestName}" />
                    <DataGridTextColumn Header="الرمز" Binding="{Binding TestCode}" />
                    <DataGridTextColumn Header="النتيجة" Binding="{Binding ResultValue}" />
                    <DataGridTextColumn Header="العلامة" Binding="{Binding ResultFlag}" />
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="طباعة" Margin="0,0,8,0"/>
        </StackPanel>
    </Grid>
</UserControl>