<UserControl x:Class="TopLab.Presentation.Views.Patients.PatientResultSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Patient Header -->
        <Border Grid.Row="0" Background="#F0F4F8" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding PatientName}" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding LabId, StringFormat='الرقم المعملي: {0}'}" FontSize="14" Foreground="Gray"/>
                <TextBlock Text="{Binding TotalCount, StringFormat='عدد التحاليل: {0}'}" FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <!-- Loading -->
        <ProgressBar Grid.Row="1" Height="6" IsIndeterminate="True" Margin="0,0,0,8">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>

        <!-- Empty state -->
        <TextBlock Grid.Row="2" Text="لا تحاليل للزيارة" Foreground="#666666" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowEmpty}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Results Grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding PatientSheet.Lines}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  RowHeaderWidth="0">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowEmpty}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            <DataGrid.Columns>
                <DataGridTextColumn Header="اسم التحليل" Binding="{Binding TestName}" Width="*"/>
                <DataGridTextColumn Header="الرمز" Binding="{Binding TestCode}" Width="80"/>
                <DataGridTextColumn Header="النتيجة" Binding="{Binding ResultValue}" Width="100"/>
                <DataGridTextColumn Header="العلامة" Binding="{Binding ResultFlag}" Width="70"/>
                <DataGridTextColumn Header="ملاحظات" Binding="{Binding Notes}" Width="120"/>
                <DataGridCheckBoxColumn Header="مُعتمد" Binding="{Binding IsReviewed}" Width="70"/>
                <DataGridCheckBoxColumn Header="مطبوع" Binding="{Binding IsPrinted}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Messages -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Green" Margin="0,8,0,0"/>
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,24,0,0"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="طباعة جماعية" Margin="0,0,8,0" Padding="12,6"
                    Command="{Binding BulkPrintCommand}"/>
            <Button Content="تصدير PDF" Margin="0,0,8,0" Padding="12,6"
                    Command="{Binding ExportPdfCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
