<UserControl x:Class="TopLab.Presentation.Views.Lab.SampleCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <DockPanel Margin="12">
        <!-- Day filter + paging + refresh -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="اليوم:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <DatePicker SelectedDate="{Binding DayDateTime}" Width="140" Margin="0,0,8,0" />
            <TextBlock Text="الصفحة:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="50" Text="{Binding Page, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
            <TextBlock Text="حجم الصفحة:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="60" Text="{Binding PageSize, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
            <Button Content="تحديث" Width="80" Margin="0,0,8,0" Command="{Binding RefreshCommand}" />
        </StackPanel>

        <!-- Empty state: operational-success empty text -->
        <TextBlock DockPanel.Dock="Top" Text="لا عينات معلقة" Foreground="#666666" Margin="0,8,0,0">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowEmpty}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Loading indicator -->
        <ProgressBar DockPanel.Dock="Top" Height="6" IsIndeterminate="True" Margin="0,4,0,0">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>

        <!-- Error strip -->
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,8,0,0" />

        <!-- Read-only worklist grid -->
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,8,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="المريض" Binding="{Binding FullName}" Width="*" />
                <DataGridTextColumn Header="Lab ID" Binding="{Binding LabId}" Width="100" />
                <DataGridTextColumn Header="تاريخ التسجيل" Binding="{Binding RegistrationDateUtc, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="160" />
                <DataGridTextColumn Header="عدد العينات المعلقة" Binding="{Binding UndrawnCount}" Width="140" />
                <DataGridTemplateColumn Header="إجراءات" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="فتح" Width="60" Margin="2"
                                    Command="{Binding DataContext.OpenPatientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding DataContext.OpenPatientEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
