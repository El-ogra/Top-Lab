# S-05 — P4 UI Pass: PatientSearch (M08), ReportProduction (M07), ResultDelivery (M09) & SentOutSamples (M16) — Complete Six-Slice Execution Plan

**Target Framework:** net8.0 / net8.0-windows
**Architecture:** Clean Architecture (Domain, Application, Infrastructure, Presentation/WPF)
**Patterns:** MediatR 12.5.0 CQRS, FluentValidation 12.1.1, EF Core 8.0.30 on SQL Server, xUnit 2.9.3 + coverlet, hand-rolled MVVM (`ViewModelBase.SetProperty`), `INavigationService` + `ContentControl`+`DataTemplate` shell navigation, `IDialogService`
**Workstream ID:** S-05 (S-series per S-00/S-01/S-02/S-03/S-04 Naming Rationale: a cross-cutting Presentation workstream, not a numbered feature module; preserves the `ID.md` / `ID-memory.md` two-artifact shape and the commit-message format). In-repository placement: `Docs/OpenCode/S-05.md` + `Docs/OpenCode/S-05-memory.md`.
**Authoritative source plan:** «Fourth Pass Plan.md» (the "P4 plan") — **as corrected by the independent audit recorded in `S-05-memory.md` §"Corrections Log"**; this plan reorganises the corrected P4 plan into six executable slices without changing any requirement.
**Authoritative commit:** `0e66b014d5f3889e0ec11e15398f00ce967e196d` («[S-04] Slice 7/8: Culture results screens + worklist routing — loop-engineering», 2026-09-18 10:37:43 +0300) — identical to the P4 plan's pinned commit; re-verified as `main` HEAD during the audit (`git rev-parse origin/HEAD` matched; `git status --porcelain` returned 0 lines). Must be HEAD at Slice 0 Stage 1.
**Source priority (from the P4 plan, binding):** code at the pinned commit > «إبتدائي الواجهات والنوافذ الرسوميه.md» > «تمهيدي الواجهات والنوافذ الرسوميه.md» > «الوصول الى خطه التنفيذ.md» (methodology only) > RLS files (documented inspiration only) > general knowledge (lowest). Where any document conflicts with live code, **the live code is the judge**.

---

## 0. Overall Objective

Ship the complete Presentation layer for the four P4 operational modules — PatientSearch (M08), ReportProduction (M07), ResultDelivery (M09), SentOutSamples (M16) — whose backends are already fully implemented and verified at the pinned commit. Settle the two remaining navigation debts: the disabled «بحث عن مريض» and «تسليم نتائج المرضى» gateways in `PatientsHubViewModel`. No new backend artifact, no migration, no permission change is authorised anywhere in this workstream.

## 1. Execution Philosophy (repo-native loop-engineering)

Strict sequential slices, each closed by a Validation Gate and a local commit. UI behaviour is verified manually per plan (no UI test harness exists in the repo; do not invent one). Every Arabic user-facing string that has a backend counterpart is copied verbatim from the backend file (frozen message tables in §2). UI-only strings are created at execution time and recorded in the memory file's register.

## 2. Verified Baseline (independently re-verified at `0e66b01` during the audit — ground truth, do not re-derive)

- **Presentation absence (confirmed by full tree inspection):** no View/ViewModel exists for any of the four modules (`find` over `src/TopLab.Presentation` for PatientSearch/ResultDelivery/SentOut/CombinedReport/BlankReport/HistoryReport/InsertHistory returns zero files). `DependencyInjection.cs` registers no ViewModel for them (73 lines of `AddTransient` inspected). No `DataTemplate` for them in `MainWindow.xaml`.
- **Navigation debt:** `src/TopLab.Presentation/ViewModels/Patients/PatientsHubViewModel.cs` — `OpenSearchPatientCommand = new RelayCommand(_ => { })` and `OpenDeliverResultsCommand = new RelayCommand(_ => { })`; `_searchPatientEnabled` and `_deliverResultsEnabled` never set to `true` (default `false`). Buttons «بحث عن مريض» (`PatientsHubView.xaml` lines 14–15, bound to `SearchPatientEnabled`) and «تسليم نتائج المرضى» (lines 16–17, bound to `DeliverResultsEnabled`). The M04 gateway («إدخال نتائج التحاليل») is already enabled and wired to `ResultsWorklistViewModel` (S-04 Slice 2).
- **Shell debt (out of scope, recorded for consistency):** `src/TopLab.Presentation/ViewModels/Shell/ShellViewModel.cs` — 12 navigation titles; «الحسابات» explicitly disabled (`IsEnabled = t != "الحسابات"`, line 171); «الأدوات»/«الإحصائيات»/«النظام» fall through `// Future: navigate to feature` (line 234). D3 comment (lines 167–168): official ExternalEntities entry lives under «الحسابات», disabled until P5; temporary tagged route via `SettingsDashboardViewModel.OpenExternalEntitiesAsync()` covers the gap.
- **Cross-cutting surfaces (verbatim-verified):** `INavigationService` (`NavigateTo<TViewModel>()`, `NavigateTo(ViewModelBase)`, `CurrentViewModel`, `Navigated`); the prevailing idiom `_navigation.NavigateTo<XViewModel>(); if (_navigation.CurrentViewModel is XViewModel vm) { await vm.LoadAsync(...); }`; `IDialogService` (`ShowConfirmationAsync(title, message)`, `ShowErrorAsync(message)`, `ShowSecondaryPasswordDialogAsync()`, `PickBackupFolderAsync`, `PickBackupFileAsync`, `PickPdfSavePathAsync(suggestedFileName)`); `ResultErrorPresenter` (Validation/NotFound/Conflict → message as-is; Forbidden → «أنت لا تملك الصلاحية لهذا العمل راجع مدير النظام»; Unexpected → «حدث خطأ غير متوقع. حاول مرة أخرى.»).
- **M08 backend (verified):** `SearchPatientsGlobalQuery(Text?, Page=1, PageSize=50)` (no `IAuthorizedRequest`); `GetPatientByLabIdQuery(LabId)`; `GetVisitHistoryQuery(PatientId)`; `GetVisitDetailQuery(PatientId)`; DTOs in `Features/PatientSearch/Common/PatientSearchDtos.cs`; verbatim messages: «معاملات الترقيم غير صالحة.» (Page ≥ 1, PageSize ∈ [1,500]), «نص البحث يجب ألا يتجاوز 200 حرفًا.», «كود المعمل مطلوب.», «كود المعمل يجب ألا يتجاوز 30 حرفًا.», «لا يوجد مريض بهذا الكود.», «المريض غير موجود.», «معرف المريض غير صالح.», «سجل إعدادات التقرير مفقود.».
- **M07 backend (verified):** queries `GetCombinableTestsQuery` (handler filters `IsReviewed`), `GetPatientTestHistoryQuery`, `GetMultiPatientHistoryQuery` («قائمة المرضى مطلوبة.»), `GetSeparateHistoryReportQuery`; commands `BuildCombinedReportCommand` («قائمة التحاليل مطلوبة.»), `PrintCombinedReportCommand` (`PRINT_RESULTS`; «معرّف المريض غير صالح.»; balance gate Conflict «يوجد رصيد متبقٍ على حساب المريض؛ لا يمكن الطباعة.»), `BuildBlankReportCommand`, `PrintBlankReportCommand` (`PRINT_RESULTS`; no balance gate, no `MarkPrinted` — OD-07-E), `InsertHistoryResultCommand`, `AutoInsertHistoryCommand`, `PrintHistoryReportCommand` («لا يمكن طباعة نتيجة غير معتمدة.» + balance gate + `MarkPrinted`); `DomainFailureTranslator`: «لا يمكن إدراج نتيجة غير معتمدة في التقرير.» / «لا يمكن تكرار نفس التحليل في التقرير.» / «تعذر تحديد هوية المريض للتاريخ المرضي.»; `IReportPrintingService.PrintReportAsync(token)` with `ReportPrintEnvelope` kinds `Combined`/`Blank`/`History`.
- **M09 backend (verified):** `GetUndeliveredResultsQuery(From?, To?, Page=1, PageSize=50)` + `IAuthorizedRequest("DELIVER_RESULTS")` («بداية الفترة يجب ألا تتجاوز نهايتها.» / «معاملات الترقيم غير صالحة.»); `GetDeliveryGridQuery(PatientId)` («معرّف المريض غير صالح.»); `GetDeliveryAccountQuery(PatientId)` (delegates to `PatientBillingReader.ReadAccount` — confirmed by direct read); `DeliverWithSettlementCommand(PatientId, PatientTestIds, SettleAmount?, SettleInFull)` (atomic, single `SaveChanges`; «حدد نتيجة واحدة على الأقل للتسليم.» / «مبلغ التسوية غير صالح.» / «لا يوجد رصيد مستحق للتسوية.»); `PatientTest.MarkDelivered` guard «Result not printed.» (line 195) → translated «النتيجة غير مطبوعة.» in `ResultDelivery/Common/DomainFailureTranslator.cs`.
- **M16 backend (verified):** `SendSampleOutCommand(PatientTestId, ExternalLabEntityId, CostPrice?, PatientPrice?)` (`CASH_DISBURSE_DEPOSIT` id=11; «التحليل غير موجود» / «الجهة الخارجية غير موجودة.» / «السعر يجب ألا يكون سالبًا.» / «هذا التحليل غير مهيأ للإرسال للخارج.» / «الجهة المختارة ليست معملًا خارجيًا.» / «تم إرسال هذه العينة مسبقًا.»; defaults `test.SentOutCostPrice ?? 0m` and `test.PatientPrice`); `RecordSentOutPaymentCommand` («العينة المُرسَلة غير موجودة.» / «مبلغ الدفع يجب أن يكون أكبر من صفر.» / «مبلغ الدفع يتجاوز المتبقي.»); `SettleSentOutInFullCommand` («لا يوجد رصيد مستحق للتسوية.»); `GetSentOutSamplesQuery(From?, To?, ExternalLabEntityId?, Page, PageSize)` (PageSize ∈ [1,100]); `GetSentOutLabAccountQuery(ExternalLabEntityId, From?, To?)`.
- **D10 source (verified — decision closed, §3):** `EntityType { TreatingDoctor = 0, ReferralOrContract = 1, PartnerLab = 2 }` (`Domain/Common/Enums/EntityType.cs`); `SearchExternalEntitiesQuery(EntityType?, SearchTerm?, Page, PageSize)` filters by type (`if (request.EntityType.HasValue) query = query.Where(e => e.EntityType == request.EntityType.Value)`); `ExternalEntityListItemDto` carries `Id`/`Name`/`EntityType`; `ExternalEntityPickerViewModel` + `ExternalEntityPickerWindow.xaml` exist with `TypeFilters` including `PartnerLab`.
- **Frozen message tables:** see each slice's spec; the full verbatim sets are reproduced in the memory file's Confirmed Facts.

## 3. Settled Decisions (binding; do NOT reopen)

1. **D10 — مصدر قائمة المعامل الخارجية في M16:** **«قرار نهائي بتفويض من المالك — اتخذه الوكيل بناءً على تحليل الكود»**. قائمة المعامل في حوار إرسال العينة وفلتر المعمل في شاشة العينات المرسلة مصدرها سجلات ExternalEntities (M14) المُرشَّحة بالنوع `EntityType.PartnerLab` عبر الاستعلام القائم `SearchExternalEntitiesQuery(EntityType: EntityType.PartnerLab, SearchTerm: null, Page: 1, PageSize: 100)`؛ يُعرض من `ExternalEntityListItemDto` الحقلان `Id` (يُمرَّر إلى `SendSampleOutCommand.ExternalLabEntityId`) و`Name` (نص العرض). لا يلزم إنشاء أي استعلام جديد. الأساس: المسار (a) — فحص الكود الحي — حاسم بثلاثة أدلة: (1) `PartnerLab = 2` قيمة نوع صريحة؛ (2) `SendSampleOutCommandHandler` يفرض `EntityType.PartnerLab` ويرفض غيره بـ «الجهة المختارة ليست معملًا خارجيًا.»؛ (3) `SearchExternalEntitiesQueryHandler` يدعم الترشيح بالنوع فعلياً ومنتقي واجهة قائم (`ExternalEntityPickerViewModel`) مبني عليه ويتضمن خيار `PartnerLab`. البديل المعتمد عند الحاجة الحوارية: إعادة استخدام `ExternalEntityPickerViewModel` مع قفل فلتر النوع على `PartnerLab`.
2. **M16 temporary entry point (mechanism):** يتبع آلية D3 المحسومة كودياً — مسار مؤقت موسوم صراحة عبر لوحة الإعدادات حتى يهبط قسم «الحسابات» في P5. **الموضع الدقيق داخل اللوحة: «بانتظار قرار المالك — غير مُدرج في القائمة الأصلية»** — مسجَّل لا محسوم؛ الوكيل المنفذ يتوقف ويبلغ عند Stage 4 من Slice 5 إن لم يكن المالك قد حسم الموضع (انظر Slice 5 Blocking conditions).
3. **لا صلاحيات جديدة:** `PRINT_RESULTS` (M07)، `DELIVER_RESULTS` (M09)، `CASH_DISBURSE_DEPOSIT` (M16) كما هي مزروعة؛ الواجهة تعرض رفض الصلاحية حرفياً عبر `ResultErrorPresenter`.

**Slice order (dependency-ordered; internal module sequence M08 → M07 → M09 → M16 binding):**

| Slice | Module | Content | Depends on |
|---|---|---|---|
| S0 | M08 | PatientSearchView (search screen) + «بحث عن مريض» gateway activation | M02/M03/M04 backends (done P2/P3) |
| S1 | M08 | PatientVisitHistoryView (master-detail) + cross-module button skeletons | S0 |
| S2 | M07 | CombinedReportView + InsertHistoryDialog + enable «التقارير» from S1 | S1; M04–M06 backends (done P3) |
| S3 | M07 | BlankReportView + HistoryReportsView (3 modes) | S2 |
| S4 | M09 | ResultDeliveryView + DeliveryHandoverView + «تسليم نتائج المرضى» gateway + enable «التسليم» from S1 | S1; M03+M04 backends (done P2/P3) |
| S5 | M16 | SentOutSamplesView + SendSampleOutDialog (D10 closed) + SentOutLabAccountView + temporary entry point + enable «إرسال خارجياً» from S1 | S1; M14+M02 backends (done P1/P2) |

Cross-module open affordances ship disabled until their target slice lands (no-half-wired-state rule, per S-04 precedent).

## 4. The Six Slices

### Slice 0 — Patient Search screen (M08) + gateway activation

**Scope:** New `ViewModels/Patients/PatientSearchViewModel.cs` + `Views/Patients/PatientSearchView.xaml`; DI transient registration; `MainWindow.xaml` DataTemplate; **settle the navigation debt:** `PatientsHubViewModel.SearchPatientEnabled = true`, `OpenSearchPatientCommand` wired to `NavigateTo<PatientSearchViewModel>()` (+ `LoadAsync` via the `CurrentViewModel is` cast idiom).

**Spec (corrected P4 plan §1.4/شاشة 1, audited):** نص البحث `SearchText` → `SearchPatientsGlobalQuery.Text` (اختياري، ≤ 200 → «نص البحث يجب ألا يتجاوز 200 حرفًا.»)؛ كود المعمل `LabIdText` → `GetPatientByLabIdQuery.LabId` (إلزامي عند «جلب بالكود»، ≤ 30 → «كود المعمل مطلوب.» / «كود المعمل يجب ألا يتجاوز 30 حرفًا.»)؛ زر «بحث» → `SearchPatientsGlobalQuery`؛ زر «جلب بالكود» → `GetPatientByLabIdQuery` («لا يوجد مريض بهذا الكود.»)؛ جدول النتائج من `PatientSearchHitDto` بالأعمدة: `FullName`، `LabId`، `Title`، `Sex`، `AgeValue`+`AgeUnit`، `NationalId`، `PhoneNumbers`، `AccountType`، `IsVip`، `AggregateStatus` (S1..S7)، `RegistrationDateUtc`، `TestCount` + عدّاد نتائج؛ ترقيم «التالي/السابق» (Page ≥ 1، PageSize ∈ [1,500]، افتراضي 50 → «معاملات الترقيم غير صالحة.»)؛ فتح الملف (زر صف/نقر مزدوج) → `NavigateTo<PatientVisitHistoryViewModel>()` + `LoadAsync(patientId)` — يُشحن معطلاً حتى Slice 1. «جلب بالكود» الناجح ينتقل مباشرة إلى `PatientVisitHistoryViewModel` حاملاً `VisitHistoryDto` (يُفعَّل في Slice 1). نقطة الخروج: «رجوع» → `NavigateTo<PatientsHubViewModel>()`. الحالات الأربع: Loading (`IsBusy`، نمط `ResultsWorklistViewModel.IsBusy`) / Empty («لا توجد نتائج مطابقة.» — نص واجهة مُنشأ، يُسجَّل في سجل النصوص) / Error عبر `ResultErrorPresenter` بالنصوص الحرفية أعلاه / Confirmation غير منطبقة (قراءة فقط — مبرَّر: كل أوامر M08 استعلامات).

**Verification Gate — VG-01:**
1. Build 0/0; full suite green; zero-drift gate (`git diff --stat src/TopLab.Domain/ src/TopLab.Application/ src/TopLab.Infrastructure/` empty; `dotnet ef migrations has-pending-model-changes` → "No changes").
2. Grep/inspection: `SearchPatientsGlobalQuery` + `GetPatientByLabIdQuery` have non-test consumers; `PatientsHubViewModel` diff limited to the M08 gateway (enabled + wired command); open-patient affordance present and disabled; no new backend file.
3. Manual (recorded): «المرضى» → «بحث عن مريض» enabled and opens the screen; search by name/phone/lab-code/national-id returns rows; paging respects limits; empty result shows the empty text; over-200-char text shows the verbatim validation message; «رجوع» returns to the hub.

**Blocking conditions:** any need for a new backend type is a STOP-and-report. Commit: `[S-05] Slice 0/6: Patient search screen + gateway activation — loop-engineering`.

### Slice 1 — Patient visit history (M08, master-detail) + cross-module skeletons

**Scope:** New `PatientVisitHistoryViewModel` + `PatientVisitHistoryView` (Master-Detail: جدول زيارات + لوحة تفاصيل الزيارة)؛ تفعيل فتح الملف من Slice 0 ومسار «جلب بالكود» المباشر؛ أزرار العبور.

**Spec (corrected P4 plan §1.4/شاشة 2, audited):** رأس ديموغرافي من `VisitHistoryDto.PatientFullName`/`LabId`؛ جدول الزيارات من `VisitHistoryDto.Visits` (`VisitSummaryDto`: `RegistrationDateUtc`، `AccountType`، `IsVip`، `TestCount`، `ResultsEntered`/`ReviewedCount`/`PrintedCount`/`DeliveredCount`، رايات `AllResultsEntered`/`AllReviewed`/`AllPrinted`/`AllDelivered`، `AggregateStatus`، `Balance`)؛ سطر معلوماتي لـ `HistorySortMode` + `HistoryAutoDisplayEnabled` (صدى إعدادات M22 — عرض فقط)؛ تفاصيل الزيارة المحددة عبر `GetVisitDetailQuery(PatientId)` → `VisitDetailDto` (ديموغرافيا + `PhoneNumbers` + `MedicalConditionNames` + `Balance`) وجدول تحاليل من `VisitTestLineDto` (`TestName`، `TestCode`، `PriceAtOrderTime`، `IsSampleDrawn`، `IsTakenOutsideLab`، `ResultValue`، `ResultFlag`، `IsReviewed`، `IsPrinted`، `PrintCount`، `IsDelivered`). أزرار العبور: «فتح الحساب» → `NavigateTo<PatientAccountViewModel>()` + `LoadAsync(patientId)` (سطح قائم مؤكد — يُفعَّل فوراً)؛ «كشف النتائج» → `PatientResultSheetViewModel` بتحميل `patientId` (`LoadCommand` يقبل `param is int patientId` — سطح قائم مؤكد، يُفعَّل فوراً)؛ «التقارير» → يُشحن معطلاً حتى Slice 2؛ «التسليم» → يُشحن معطلاً حتى Slice 4؛ «إرسال خارجياً» على سطر تحليل (يحمل `PatientTestId` من `VisitTestLineDto`) → يُشحن معطلاً حتى Slice 5. التحقق الوحيد: `PatientId > 0` → «معرف المريض غير صالح.». الحالات الأربع: Loading (`IsBusy` أثناء الاستعلامين) / Empty (زيارة بلا تحاليل → «لا توجد تحاليل في هذه الزيارة.» — نص واجهة مُنشأ يُسجَّل) / Error («المريض غير موجود.» / «سجل إعدادات التقرير مفقود.» عبر `ResultErrorPresenter`) / Confirmation غير منطبقة (قراءة فقط — مبرَّر). لا حفظ؛ الإلغاء = «رجوع» → `NavigateTo<PatientSearchViewModel>()`.

**Verification Gate — VG-02:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: `GetVisitHistoryQuery` + `GetVisitDetailQuery` consumed; «فتح الحساب» و«كشف النتائج» wired to the existing VMs; «التقارير»/«التسليم»/«إرسال خارجياً» present and disabled; no new backend file.
3. Manual (recorded): open from search and from direct lab-id fetch shows all visits newest-first; selecting a visit loads its detail; balance renders per `BalanceProbe` formula; both live cross-buttons navigate correctly; «رجوع» returns to search.

**Blocking conditions:** any DTO field missing from the live queries is a STOP-and-report (audit says none are). Commit: `[S-05] Slice 1/6: Patient visit history master-detail screen — loop-engineering`.

### Slice 2 — Combined report + insert-history dialog (M07)

**Scope:** New `CombinedReportViewModel` + `CombinedReportView`؛ حوار `InsertHistoryDialogViewModel` + `InsertHistoryDialog` (Window)؛ تفعيل زر «التقارير» من Slice 1؛ زر «تقرير مدموج» في `PatientResultSheetView` (M04) — إضافة زر Requires creation هناك.

**Spec (corrected P4 plan §2.4/شاشة 1 وشاشة 4, audited):** قائمة التحاليل القابلة للدمج من `GetCombinableTestsQuery(PatientId)` → `CombinableTestDto` (اسم/كود/`ResultKind`) مع CheckBox اختيار؛ أزرار «أعلى/أسفل» تعيد ترتيب القائمة المحددة في الـ VM (الترتيب النهائي يُمرَّر كـ `OrderedPatientTestIds` — إعادة الترتيب مسؤولية الواجهة؛ `CombinedReportSelection.MoveUp/MoveDown` داخلية في الـ Handler)؛ عدّاد المحدد؛ «بناء المعاينة» → `BuildCombinedReportCommand` → عرض `CombinedReportDto.Lines` (قيمة/راية/`FrozenRangeText`/سطور بروفايل/ملخص مزرعة)؛ لا اختيار → «قائمة التحاليل مطلوبة.»؛ «طباعة» → `PrintCombinedReportCommand` (صلاحية `PRINT_RESULTS`؛ الأخطاء الحرفية: «معرّف المريض غير صالح.» / «قائمة التحاليل مطلوبة.» / «يوجد رصيد متبقٍ على حساب المريض؛ لا يمكن الطباعة.» / «لا يمكن طباعة نتيجة غير معتمدة.»؛ Forbidden → «أنت لا تملك الصلاحية لهذا العمل راجع مدير النظام»)؛ «إدراج من التاريخ» → يفتح `InsertHistoryDialog` بسطر `PatientTestId` مستهدف: جدول تاريخ المريض لنفس التحليل من `GetPatientTestHistoryQuery` → اختيار سطر مصدر → «إدراج» → `InsertHistoryResultCommand(PatientTestId, SourcePatientTestId)` («لا يمكن إدراج نتيجة غير معتمدة في التقرير.» / «لا يمكن تكرار نفس التحليل في التقرير.» / «التحليل غير موجود») خلف تأكيد عبر `IDialogService.ShowConfirmationAsync` (نص التأكيد مُنشأ يُسجَّل)، والنجاح يحدث المعاينة بالـ `CombinedReportDto` المُعاد؛ «إدراج تلقائي» → `AutoInsertHistoryCommand(PatientTestId)` **خلف تأكيد إلزامي** (يعدّل تقريراً رسمياً؛ نص التأكيد مُنشأ يُسجَّل). الحالات الأربع: Loading (`IsBusy` أثناء الاستعلام/البناء/الطباعة) / Empty («لا توجد تحاليل معتمدة قابلة للدمج لهذا المريض.» و«لا نتائج تاريخية لهذا التحليل.» — نصوص مُنشأة تُسجَّل) / Error (كل الرسائل أعلاه) / Confirmation (الإدراج اليدوي والتلقائي). نقطة الخروج: «رجوع» إلى الشاشة الفاتحة.

**Verification Gate — VG-03:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: `GetCombinableTestsQuery`/`BuildCombinedReportCommand`/`PrintCombinedReportCommand`/`InsertHistoryResultCommand`/`AutoInsertHistoryCommand`/`GetPatientTestHistoryQuery` all consumed; both insertions behind `ShowConfirmationAsync`; «التقارير» button from Slice 1 enabled; no new permission string (`grep -r "PRINT_RESULTS" src/TopLab.Presentation/` references only the backend constant — no re-declared code).
3. Manual (recorded): build with no selection refuses verbatim; reorder then preview reflects order; balance-blocked print shows verbatim gate message; manual insert of an unreviewed result refuses verbatim; auto-insert confirms then inserts.

**Blocking conditions:** any behaviour change request to the backend commands is a STOP-and-report. Commit: `[S-05] Slice 2/6: Combined report screen + insert-history dialog — loop-engineering`.

### Slice 3 — Blank report + history reports (M07)

**Scope:** New `BlankReportViewModel` + `BlankReportView`؛ `HistoryReportsViewModel` + `HistoryReportsView` بثلاثة أوضاع (مريض واحد / متعدد المرضى / منفصل)؛ الدخول من `CombinedReportView` (تبويب/زر) ومن ملف المريض.

**Spec (corrected P4 plan §2.4/شاشتَي 2 و3, audited):** التقرير الفارغ: «بناء» → `BuildBlankReportCommand(PatientId)` → عرض `BlankReportDto` الستة (`PatientFullName`/`LabId`/`Sex`/`AgeValue`+`AgeUnit`/`TreatingDoctorName`/`ReferralEntityName`)؛ «طباعة» → `PrintBlankReportCommand` (`PRINT_RESULTS`؛ **لا بوابة رصيد ولا أثر طباعة** — سلوك كودي محسوم OD-07-E؛ لا تأكيد)؛ حالة «لم يُبنَ تقرير بعد» قبل أول بناء (نص مُنشأ يُسجَّل). تقارير التاريخ: وضع مريض واحد → `GetPatientTestHistoryQuery(PatientId)` → `PatientHistoryDto` مع عرض `HistorySortMode` (`ByLabCode`/`ByPatientName` — يُعرض ولا يُحرَّر) و`HistoryAutoDisplayEnabled` وجدول `HistoryEntryDto`؛ وضع متعدد المرضى → إضافة متتالية للمرضى (بالكود/البحث عبر شاشة M08) ثم `GetMultiPatientHistoryQuery(PatientIds)` («قائمة المرضى مطلوبة.»)؛ وضع منفصل → `GetSeparateHistoryReportQuery(PatientId)` للمعاينة + `PrintHistoryReportCommand(PatientId)` للطباعة («لا يمكن طباعة نتيجة غير معتمدة.» / بوابة الرصيد؛ الطباعة التاريخية تستدعي `MarkPrinted` — تأكيد مسبق مفضَّل عبر `ShowConfirmationAsync`، نص مُنشأ يُسجَّل). الحالات الأربع: Loading / Empty («لا يوجد تاريخ مرضي لهذا المريض.» — مُنشأ يُسجَّل) / Error (الموجودة + «تعذر تحديد هوية المريض للتاريخ المرضي.») / Confirmation (الطباعة التاريخية فقط).

**Verification Gate — VG-04:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: `BuildBlankReportCommand`/`PrintBlankReportCommand`/`GetPatientTestHistoryQuery`/`GetMultiPatientHistoryQuery`/`GetSeparateHistoryReportQuery`/`PrintHistoryReportCommand` consumed; blank print path contains no balance check UI-side (backend-settled); three modes selectable.
3. Manual (recorded): blank report builds and prints without balance interference; multi-patient mode refuses empty list verbatim; separate-mode print of a patient with unreviewed-only results refuses verbatim; history print confirms then marks printed.

**Blocking conditions:** any request to add Word export is a STOP-and-report (Top-Lab is PDF-only — settled). Commit: `[S-05] Slice 3/6: Blank report + history reports screens — loop-engineering`.

### Slice 4 — Result delivery (M09) + gateway activation

**Scope:** New `ResultDeliveryViewModel` + `ResultDeliveryView` (قائمة غير المسلَّم)؛ `DeliveryHandoverViewModel` + `DeliveryHandoverView` (حساب التسليم + الشبكة)؛ **settle the second navigation debt:** `PatientsHubViewModel.DeliverResultsEnabled = true`، `OpenDeliverResultsCommand` wired to `NavigateTo<ResultDeliveryViewModel>()` + `LoadAsync`؛ تفعيل زر «التسليم» من Slice 1.

**Spec (corrected P4 plan §3.4/شاشتَي 1 و2, audited):** الشاشة 1: فترة «من/إلى» (`DateOnly?`، افتراضي اليوم UTC؛ من ≤ إلى وإلا «بداية الفترة يجب ألا تتجاوز نهايتها.»)؛ «عرض» → `GetUndeliveredResultsQuery` (Page/PageSize افتراضي 1/50 → «معاملات الترقيم غير صالحة.»)؛ الجدول من `UndeliveredPatientRowDto` (`PatientFullName`، `LabId`، `RegistrationDateUtc`، `VisitStatus` S1..S7، `UndeliveredCount`)؛ فتح صف → `DeliveryHandoverView`. الشاشة 2: لوحة الحساب من `GetDeliveryAccountQuery(PatientId)` → `TotalCharged`/`TotalPaid`/`Balance`/**`RemainingToLab`**/**`RemainingToPatient`** بارزة («المريض غير موجود.»)؛ شبكة النتائج من `GetDeliveryGridQuery` → `DeliveryGridRowDto`؛ عمود CheckBox التسليم **يُفعَّل فقط لسطر `IsPrinted == true && IsDelivered == false`** (الحارس «Result not printed.» → «النتيجة غير مطبوعة.»)؛ حقل «مبلغ التسوية» (> 0 عند إدخاله وإلا «مبلغ التسوية غير صالح.»؛ يُمنع مع «خلاص»)؛ CheckBox «تسوية كاملة (خلاص)» (`SettleInFull`؛ يتنافى مع مبلغ موجب؛ يفشل عند رصيد ≤ 0 بـ «لا يوجد رصيد مستحق للتسوية.»)؛ زر «تسليم وتسوية» → `DeliverWithSettlementCommand(PatientId, PatientTestIds, SettleAmount?, SettleInFull)` — **فعل واحد ذري** (`SaveChanges` واحد؛ فشل التسوية يمنع التسليم والعكس) خلف **تأكيد واحد حرج** عبر `ShowConfirmationAsync` يعرض عدد النتائج المحددة + المبلغ النهائي (دفعة/خلاص/بلا تسوية) — نص التأكيد مُنشأ يُسجَّل؛ «حدد نتيجة واحدة على الأقل للتسليم.» عند اختيار فارغ. ما بعد النجاح: إعادة الاستعلامين لتحديث الشبكة والحساب ثم الرجوع لتحديث قائمة الشاشة 1. طباعة إيصال التسوية عبر `PrintReceiptCommand` القائم في M03 خطوة اختيارية لاحقة (Inference معلَّم — لا أمر طباعة داخل `DeliverWithSettlementCommandHandler`). الحالات الأربع كاملة: Loading / Empty («لا توجد نتائج غير مسلَّمة في هذه الفترة.» / «لا توجد تحاليل لهذا المريض.» — مُنشأة تُسجَّل) / Error (الموجودة + Forbidden حرفياً) / Confirmation (الحرجة أعلاه).

**Verification Gate — VG-05:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: all three queries + `DeliverWithSettlementCommand` consumed; `PatientsHubViewModel` diff limited to the M09 gateway; deliver CheckBox disabled for unprinted/delivered rows by trigger; single confirmation before the atomic command; no re-implementation of account math UI-side (`ReadAccount` delegation respected).
3. Manual (recorded): «تسليم نتائج المرضى» enabled and opens the list; period default = today; settlement-in-full on zero-balance patient refuses verbatim; partial payment + deliver succeeds atomically; forbidden user sees the verbatim permission message.

**Blocking conditions:** any request to split deliver/settle into two actions is a STOP-and-report (atomicity is backend-settled). Commit: `[S-05] Slice 4/6: Result delivery screens + gateway activation — loop-engineering`.

### Slice 5 — Sent-out samples (M16, D10 closed) + temporary entry point

**Scope:** New `SentOutSamplesViewModel` + `SentOutSamplesView`؛ حوار `SendSampleOutDialogViewModel` + `SendSampleOutDialog` (Window)؛ `SentOutLabAccountViewModel` + `SentOutLabAccountView`؛ تفعيل زر «إرسال خارجياً» من Slice 1؛ نقطة دخول مؤقتة موسومة بنمط D3 (قرار §3-2).

**Spec (corrected P4 plan §4.4, audited; D10 closed per §3-1):** الشاشة 1 (القائمة): فترة «من/إلى» (افتراضي اليوم؛ «بداية الفترة يجب ألا تتجاوز نهايتها.»)؛ **فلتر المعمل ComboBox مصدره `SearchExternalEntitiesQuery(EntityType = EntityType.PartnerLab, SearchTerm = null, Page = 1, PageSize = 100)` → عناصر `ExternalEntityListItemDto(Id, Name)` + خيار «الكل» (`ExternalLabEntityId = null`)** («معاملات الترقيم غير صالحة.» — PageSize ≤ 100)؛ «عرض» → `GetSentOutSamplesQuery`؛ الجدول من `SentOutSampleDto` (`PatientName`، `TestName`، `ExternalLabName`، `SentAtUtc`، `CostPrice`، `PatientPrice`، `TotalPaid`، `Remaining`، `IsFullySettled`)؛ زر صف «الحساب» → `SentOutLabAccountView`. الحوار (الإرسال): يُفتح من زر «إرسال خارجياً» على سطر تحليل في تفاصيل زيارة M08 حاملاً `PatientTestId` واسم التحليل؛ **ComboBox المعمل الخارجي بنفس مصدر D10 المحسوم** (البديل المعتمد: `ExternalEntityPickerViewModel` القائم مع قفل الفلتر على `PartnerLab`)؛ عدم الاختيار ⇒ الإرسال بـ 0 فيرجع «الجهة الخارجية غير موجودة.»؛ تكلفة المعمل وسعر التحصيل اختياريان (فارغ ⇒ `Test.SentOutCostPrice ?? 0` / `Test.PatientPrice`؛ ≥ 0 وإلا «السعر يجب ألا يكون سالبًا.»)؛ «إرسال» → `SendSampleOutCommand` (`CASH_DISBURSE_DEPOSIT`) خلف تأكيد يلخص المعمل والتكلفة (نص مُنشأ يُسجَّل)؛ الأخطاء الحرفية: «التحليل غير موجود» / «هذا التحليل غير مهيأ للإرسال للخارج.» / «الجهة المختارة ليست معملًا خارجيًا.» / «تم إرسال هذه العينة مسبقًا.». الشاشة 3 (حساب المعمل): مجاميع من `GetSentOutLabAccountQuery(ExternalLabEntityId, From, To)` (`ExternalLabName`، `SentCount`، `TotalCost`، `TotalPaid`، `Remaining`)؛ تسجيل دفعة على عينة محددة → `RecordSentOutPaymentCommand(SentOutSampleId, AmountPaid)` («مبلغ الدفع يجب أن يكون أكبر من صفر.» / «مبلغ الدفع يتجاوز المتبقي.») خلف تأكيد؛ تسوية كاملة → `SettleSentOutInFullCommand` («لا يوجد رصيد مستحق للتسوية.») خلف تأكيد (نصوص التأكيد مُنشأة تُسجَّل). الحالات الأربع لكل شاشة: Loading / Empty («لا توجد عينات مرسلة في هذه الفترة/لهذا المعمل.» / «لا توجد معامل خارجية مسجلة — أضف جهة بنوع «معمل خارجي» من إدارة الجهات.» / «لا عينات مرسلة لهذا المعمل في الفترة.» — مُنشأة تُسجَّل) / Error / Confirmation (الإرسال والدفع والتسوية). **نطاق محسوم:** شاشة حسابات المعمل تتبع M16 لا M14.

**Verification Gate — VG-06:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: all three commands + both queries consumed; **grep gate for D10:** both lab ComboBoxes sourced from `SearchExternalEntitiesQuery` with `EntityType.PartnerLab` — no new backend query anywhere in the diff; temporary entry point explicitly tagged (comment referencing D3 pattern + P5); «إرسال خارجياً» from Slice 1 enabled.
3. Manual (recorded): send a sent-out-eligible test to a PartnerLab entity succeeds; sending a non-eligible test refuses verbatim; duplicate send refuses verbatim; filter by lab isolates rows; payment above remaining refuses verbatim; full settle on settled sample refuses verbatim.

**Blocking conditions:** the exact placement of the temporary entry inside the settings dashboard is «بانتظار قرار المالك — غير مُدرج في القائمة الأصلية» (§3-2) — if the owner has not settled it by Stage 4 (Planning) of this slice, STOP-and-report requesting the placement decision; do not improvise a different host. Commit: `[S-05] Slice 5/6: Sent-out samples screens + temporary entry point — loop-engineering`.

## 5. Programme-Level Traceability (plan → slices)

| P4 plan element (audited) | Slice |
|---|---|
| M08 شاشة البحث الشامل + تفعيل بوابة «بحث عن مريض» | 0 |
| M08 شاشة ملف المريض/تاريخ الزيارات + أسطح العبور القائمة (M03/M04) | 1 |
| M07 التقرير المدموج + حوار إدراج نتيجة تاريخية | 2 |
| M07 التقرير الفارغ + تقارير التاريخ (3 أوضاع) | 3 |
| M09 قائمة غير المسلَّم + حساب التسليم الذري + تفعيل بوابة «تسليم نتائج المرضى» | 4 |
| M16 قائمة العينات + حوار الإرسال + حساب المعمل + نقطة الدخول المؤقتة (D10 محسوم) | 5 |
| قرار D10 | **closed — «قرار نهائي بتفويض من المالك — اتخذه الوكيل بناءً على تحليل الكود» (§3-1); grep-gated in Slice 5** |
| القرار الجديد (موضع الدخول المؤقت لـ M16) | **recorded, not decided — «بانتظار قرار المالك — غير مُدرج في القائمة الأصلية» (§3-2); stop-gated in Slice 5** |

**Explicit non-goals (scope containment):** no new backend command/query/DTO/validator; no migration; no `PermissionConfiguration` change; no new permission code; no NATIGH.COM electronic delivery (RLS-inspiration-only); no Word export (Top-Lab is PDF-only); no printed sent-out account statement (RLS-inspiration-only — no corresponding print query in code); no UI-managed auto-display-of-history behaviour (the `HistoryAutoDisplayEnabled` setting exists as data only); the «الحسابات»/«الأدوات»/«الإحصائيات»/«النظام» shell gaps stay as-is (P5).

---

## 6. Corrections Log (audit of the original P4 plan — see `S-05-memory.md` §"Corrections Log" for the authoritative record)

Recorded in `S-05-memory.md` (the location prior passes use for audit/verification registers). Summary: **3 corrections** — (C1) path precision for `ShellViewModel.cs` (fully qualified to `src/TopLab.Presentation/ViewModels/Shell/ShellViewModel.cs`); (C2) the §4.6 marker on D10 («قرار افتراضي من الوكيل — بانتظار موافقة المالك») replaced with the closed delegated-authority decision (§3-1 above); (C3) the «حسم القرار D10» section's status paragraph («ليس نهائياً ولا مؤكداً») replaced with the final closed text under the label «قرار نهائي بتفويض من المالك — اتخذه الوكيل بناءً على تحليل الكود», reasoning unchanged and independently re-verified. No fabricated code fact was found; every "Confirmed from code" claim audited held against the live commit. The plan's newly-discovered open decision (M16 temporary-entry placement) is preserved verbatim as «بانتظار قرار المالك — غير مُدرج في القائمة الأصلية» — recorded, not decided.
