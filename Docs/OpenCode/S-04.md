# S-04 — P3 UI Pass: SampleCollection (M21), ResultsEntry (M04), ProfileResults (M05) & CultureResults (M06) — Complete Eight-Slice Execution Plan

**Target Framework:** net8.0 / net8.0-windows
**Architecture:** Clean Architecture (Domain, Application, Infrastructure, Presentation/WPF)
**Patterns:** MediatR 12.5.0 CQRS, FluentValidation 12.1.1, EF Core 8.0.30 on SQL Server, xUnit 2.9.3 + coverlet, hand-rolled MVVM (`ViewModelBase.SetProperty`), `INavigationService` + `ContentControl`+`DataTemplate` shell navigation, `IDialogService`
**Workstream ID:** S-04 (S-series per S-00/S-01/S-02/S-03 Naming Rationale: a cross-cutting Presentation workstream, not a numbered feature module; preserves the `ID.md` / `ID-memory.md` two-artifact shape and the commit-message format). In-repository placement: `Docs/OpenCode/S-04.md` + `Docs/OpenCode/S-04-memory.md`.
**Authoritative source plan:** «خطة التنفيذ النهائية للواجهات والنوافذ الرسوميه - P3.md» (the "P3 plan") — **as corrected by the independent audit recorded in `S-04-memory.md` §"Corrections Log"**; this plan reorganises the corrected P3 plan into eight executable slices without changing any requirement.
**Authoritative commit:** `ce30962bdbfd29b17f099b8b700901750104ff88` («بعد تبديل السمات الشخصية», 2026-09-17 22:22 +0300) — identical to the P3 plan's pinned commit; re-verified as `main` HEAD during the audit (`git rev-parse HEAD` matched; `git status --porcelain` returned 0 lines). Must be HEAD at Slice 0 Stage 1.
**Source priority (from the P3 plan, binding):** code at the pinned commit > «إبتدائي الواجهات والنوافذ الرسوميه.md» > «تمهيدي الواجهات والنوافذ الرسوميه.md» > «الوصول الى خطه التنفيذ.md» (methodology only) > RLS files (documented inspiration only) > general knowledge (lowest). Where any document conflicts with live code, **the live code is the judge**.

---

## 0. Overall Objective

Ship the complete Presentation layer for the four P3 operational modules — SampleCollection (M21), ResultsEntry (M04), ProfileResults (M05), CultureResults (M06) — whose backends are already fully implemented and verified at the pinned commit. Settle the two navigation debts: the disabled «إدخال نتائج التحاليل» gateway in `PatientsHubViewModel` and the missing sample-draw entry point. No new backend artifact, no migration, no permission change is authorised anywhere in this workstream.

## 1. Execution Philosophy (repo-native loop-engineering)

Strict sequential slices, each closed by a Validation Gate and a local commit. UI behaviour is verified manually per plan (no UI test harness exists in the repo; do not invent one). Every Arabic user-facing string that has a backend counterpart is copied verbatim from the backend file (frozen message tables in §2). UI-only strings are created at execution time and recorded in the memory file's register.

## 2. Verified Baseline (independently re-verified at `ce30962` during the audit — ground truth, do not re-derive)

- **Presentation absence (confirmed by full tree inspection):** 39 ViewModels / 38 Views; none for the four modules. `DependencyInjection.cs` registers no ViewModel for them. `Views/Lab`/`ViewModels/Lab` contain catalog-definition screens only.
- **Navigation debt:** `PatientsHubViewModel.cs:36` `OpenEnterResultsCommand = new RelayCommand(_ => { });` and `EnterResultsEnabled = false` (default). `ShellViewModel.BuildNavigationItems()` titles = 12 items; «الحسابات» explicitly disabled (`IsEnabled = t != "الحسابات"`); «الأدوات»/«الإحصائيات»/«النظام» fall through `// Future: navigate to feature`.
- **«المعمل» hub:** `LabHubViewModel` is a tab container (Catalog/Groups/WorkLogs/Analytes/Profiles/Antibiotics/CultureAttachment/PriceLists/TestComments/CustomGroups) with a `LoadAsync` that loads every tab — the host for the new Sample Collection tab (settled decision, §3).
- **M21 backend (verified):** `GetPatientsWithUncollectedSamplesQuery(DateOnly? Day, int Page = 1, int PageSize = 100)`; `GetPatientTestsForDrawQuery(int PatientId)` → `SampleDrawBoardDto(PatientId, LabId, FullName, Drawn, NotDrawn)`; `MarkSampleDrawnCommand(PatientTestId, DrawnAtUtc)` → `Result<bool>` (server clock used, idempotent, outside-lab Conflict «تم تسجيل العينة كمسحوبة خارج المعمل؛ لا يمكن تعديلها من شاشة السحب»); `MarkAllSamplesDrawnForPatientCommand(PatientId)` → `Result<int>`; policy `SampleCollectionAccessPolicy.AddEditPatient = "ADD_EDIT_PATIENT"` on both write commands.
- **M04 backend (verified):** 10 commands / 3 queries + `BulkPrintPreflightQuery`; policy codes `EDIT_RESULTS/REVIEW_RESULTS/PRINT_RESULTS/DELIVER_RESULTS`; `GetResultWorklistQuery` returns `ResultWorklistItemDto` carrying both `ResultKind` (int) and `IsCultureType` (bool) — the unified worklist exists in Application already (basis of D4).
- **M05 backend (verified):** reuses `ResultsEntryAccessPolicy` (no separate policy class); `Amend` validator does NOT require `Reason` (only ≤ 500 chars).
- **M06 backend (verified):** own `CultureResultsAccessPolicy{EditResults, ReviewResults, PrintResults}`; entry grid rows sourced internally from `CultureAntibioticAttachment` + `Antibiotic`; display filter `CultureAntibioticDisplay.IsDisplayable` + `ChildAgeThresholdYears = 12`; queries are plain unauthenticated `IRequest`.
- **Frozen message tables:** see each slice's spec; the full verbatim sets are reproduced in the memory file's Confirmed Facts.

## 3. Dependency Overview & Slice Order

**Owner-settled decisions (قرار نهائي معتمد من المالك — binding; do NOT reopen):**

1. **D4 — unified Worklist:** profile (M05) and culture (M06) results open from the same R1 Results Worklist as simple results, routed by row type (`ResultKind` / `IsCultureType`). Code basis (unchanged from the audited plan): `GetResultWorklistQuery` already returns all three kinds with the routing fields, and all M05/M06 commands anchor on `PatientTestId` from the row. No standalone M05/M06 shell entries are built. No alternative is presented or built.
2. **Sample Collection entry point:** S1 is a **new tab inside the existing «المعمل» hub** (`LabHubViewModel`). No new shell button. «المعمل» branch already navigates to `LabHubViewModel` and calls `hub.LoadAsync()`.
3. **Draw-command permission scope:** no new permission. `ADD_EDIT_PATIENT` remains the access control for both draw commands, exactly as implemented. The UI shows controls and surfaces backend denials verbatim.

**Slice order (dependency-ordered; internal module sequence M21 → M04 → M05 → M06 binding):**

| Slice | Module | Content | Depends on |
|---|---|---|---|
| S0 | M21 | Sample Collection worklist tab (S1) inside «المعمل» hub + hub wiring | LabHub host (exists) |
| S1 | M21 | Patient draw board screen (S2) + S1→S2 navigation + draw-confirmation flows | S0 |
| S2 | M04 | Results Worklist (R1) + `PatientsHubViewModel` gateway activation | — |
| S3 | M04 | Simple result entry screen (R2) + clear/confirm dialog (R4) | S2 |
| S4 | M04 | Patient result sheet (R3) + review-all + print/deliver flows | S3 |
| S5 | M04 | Bulk-print dialog (R5) + PDF export (R6, SaveFileDialog) | S4 |
| S6 | M05 | Profile entry grid (P1) + verify dialogs (P5) + report preview (P2) + amendment dialogs (P3/P4) + R1 routing to P1 | S2 |
| S7 | M06 | Culture entry grid (C1) + report (C2) + verify dialog (C3) + R1 routing to C1 | S2 |

R1 routing branches (D4) land in S6/S7, after R1 exists (S2); M05/M06 detail screens depend on no M04 screen other than R1.

## 4. The Eight Slices

### Slice 0 — Sample Collection worklist tab (S1) in the «المعمل» hub

**Scope:** New `ViewModels/Lab/SampleCollectionViewModel.cs` + `Views/Lab/SampleCollectionView.xaml`; registered as a new tab property on `LabHubViewModel` (constructor injection + `LoadAsync` call, mirroring existing tabs); DI transient registration; `MainWindow.xaml` DataTemplate if the tab content requires one (follow the existing tab-content precedent — tabs are properties on the hub VM, not separate navigable VMs; bind tab content to the hub's property as the existing tab views do).

**Spec (corrected P3 plan §1.4/S1, audited):** columns المريض (`FullName`), Lab ID (`LabId`), تاريخ التسجيل (`RegistrationDateUtc`), عدد العينات المعلقة (`UndrawnCount`); day filter `Day` (default = today UTC per handler); paging `Page ≥ 1`, `PageSize ∈ [1,500]` (note: query default PageSize is **100**, worklist default is 50 — use the query's own default); refresh → `GetPatientsWithUncollectedSamplesQuery`; double-click/زر فتح → S2 (wired in Slice 1; ships disabled until then per the no-half-wired-state rule). Read-only screen — no write command from this screen. Four states: Loading / Empty («لا عينات معلقة» — created UI text, record in register; displayed positively as operational success) / Error via `ResultErrorPresenter` / Confirmation n/a. Out-of-range paging refused by backend verbatim «معاملات الترقيم غير صالحة.».

**Verification Gate — VG-01:**
1. Build 0/0; full suite green; zero-drift gate (`git diff --stat src/TopLab.Infrastructure/Persistence/` empty; `dotnet ef migrations has-pending-model-changes` → "No changes").
2. Grep/inspection: `GetPatientsWithUncollectedSamplesQuery` has a non-test consumer; tab present on `LabHubViewModel` and loaded by `LoadAsync`; no new backend file; open-patient affordance ships disabled.
3. Manual (recorded): «المعمل» → new tab shows today's undrawn patients; empty day shows the empty text; paging controls respect limits; error path shows verbatim message.

**Blocking conditions:** any need for a new backend type is a STOP-and-report. Commit: `[S-04] Slice 0/8: Sample Collection worklist tab in Lab hub — loop-engineering`.

### Slice 1 — Patient draw board (S2)

**Scope:** New `SampleDrawBoardViewModel` + `SampleDrawBoardView` (View); S1→S2 open wiring (enable the Slice-0 affordance); return to S1 with refresh.

**Spec (§1.4/S2, audited):** source `GetPatientTestsForDrawQuery` → `SampleDrawBoardDto`; patient header (`FullName`/`LabId`, read-only); «غير مسحوبة» grid from `NotDrawn` with per-row CheckBox; «مسحوبة» grid from `Drawn` read-only with `SampleDrawnAtUtc`; sample-type flags `IsUrine/IsStool/IsBlood/IsSemen/IsCsf/IsTakenOutsideLab` display-only. Actions: single mark → `MarkSampleDrawnCommand(PatientTestId, drawnAtUtc)` (server clock authoritative); «سحب الكل» → `MarkAllSamplesDrawnForPatientCommand(PatientId)` behind `IDialogService.ShowConfirmationAsync` (confirmation text created at execution, recorded). Rows with `IsTakenOutsideLab` have their CheckBox disabled proactively; backend refusal verbatim «تم تسجيل العينة كمسحوبة خارج المعمل؛ لا يمكن تعديلها من شاشة السحب». Validation: «معرّف تحليل المريض غير صالح.» / «معرّف المريض غير صالح.». Save is immediate per mark (no draft); cancel = close without effect. After a patient completes, S1 refreshes and the patient disappears. Permission: `ADD_EDIT_PATIENT` (settled — no new permission); denial surfaces verbatim via the authorization pipeline.

**Verification Gate — VG-02:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: both draw commands consumed; outside-lab CheckBox disabled by trigger; bulk action behind `ShowConfirmationAsync`; no new permission string anywhere in the diff (`grep -r "DRAW" src/` → no new code).
3. Manual (recorded): mark single → row moves to drawn grid with timestamp; «سحب الكل» confirms then marks all and returns count; outside-lab row refuses verbatim; completed patient vanishes from S1 after refresh.

**Blocking conditions:** any behaviour change request to the backend commands is a STOP-and-report. Commit: `[S-04] Slice 1/8: Patient draw board screen — loop-engineering`.

### Slice 2 — Results Worklist (R1) + PatientsHub gateway activation

**Scope:** New `ViewModels/Patients/ResultsWorklistViewModel.cs` + `Views/Patients/ResultsWorklistView.xaml`; DI + DataTemplate; **settle the navigation debt:** `PatientsHubViewModel.EnterResultsEnabled = true`, `OpenEnterResultsCommand` wired to `NavigateTo<ResultsWorklistViewModel>()` (+ `LoadAsync` via the `CurrentViewModel is` cast idiom).

**Spec (§2.4/R1, audited):** columns from `ResultWorklistItemDto`: المريض/LabId (`PatientFullName`/`LabId`), التحليل/الرمز (`TestName`/`TestCode`), نوع النتيجة (`ResultKind`), مزرعة؟ (`IsCultureType`), مسحوبة؟/خارج المعمل؟ (`IsSampleDrawn`/`IsTakenOutsideLab`), قيمة النتيجة/العلم (`ResultValue`/`ResultFlag`), مراجعة/طباعة/تسليم (`IsReviewed`/`IsPrinted`/`IsDelivered`), حالة المريض الإجمالية (`AggregateStatus` S1–S7), تاريخ التسجيل (`RegistrationDateUtc`). Filters = query parameters verbatim: `Day` (default today UTC), `HasResult`, `IsReviewed`, `TestGroupId`, `ResultKind`, `Page`/`PageSize` (default 50). Open row → routing per **settled D4**: `Simple` → R2 (Slice 3; disabled until then), `SpecializedProfile` → P1 (Slice 6; disabled until then), `Culture`/`IsCultureType` → C1 (Slice 7; disabled until then). Open patient sheet → R3 (Slice 4; disabled until then). Four states: Loading / Empty (لا نتائج مطابقة للمرشح — created text, recorded) / Error (`ResultErrorPresenter`) / Confirmation n/a.

**Verification Gate — VG-03:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: `GetResultWorklistQuery` consumed; `PatientsHubViewModel` diff limited to the gateway (enabled + wired command); all four open affordances present, non-simple ones disabled pending their slices; filter panel maps 1:1 to query parameters.
3. Manual (recorded): «المرضى» hub → «إدخال نتائج التحاليل» now enabled and opens R1; day filter defaults to today; `ResultKind` filter isolates kinds; empty filter result shows the empty text.

**Blocking conditions:** any DTO field missing from the live query is a STOP-and-report (audit says none are). Commit: `[S-04] Slice 2/8: Results Worklist + gateway activation — loop-engineering`.

### Slice 3 — Simple result entry (R2) + clear-confirmation dialog (R4)

**Scope:** New `ResultEntryViewModel` + `ResultEntryView`; R1→R2 wiring for `Simple` rows (enable from Slice 2); clear confirmation via `IDialogService` (R4 — confirmation text has no backend counterpart; created and recorded; the lock message is verbatim backend).

**Spec (§2.4/R2, §2.4/R4, audited):** source `GetResultEntryQuery` → `ResultEntryDto`; write `EnterResultCommand`. Fields: التحليل/الرمز read-only; patient context (`PatientAgeValue/PatientAgeUnit/PatientSex`) read-only (drives range matching); قيمة النتيجة mandatory («الرجاء إدخال قيمة النتيجة قبل الحفظ»); العلم optional ∈ [0,2] («قيمة العلم غير صالحة.»), auto-computed via `ResultFlagComputer` when omitted; ملاحظات optional; live ranges (`ReferenceRanges`) display; frozen snapshot (`FrozenRange`) display, never retro-changed. Actions: حفظ → `EnterResultCommand`; مسح → `ClearResultCommand` (behind confirmation); اعتماد → `ReviewResultCommand`; إلغاء اعتماد → `UnreviewResultCommand` (behind confirmation); «تحديث المرجع» → `RefreshResultReferenceRangeCommand`; طباعة → `MarkResultPrintedCommand`; تسليم → `MarkResultDeliveredCommand`. Locks (verbatim): reviewed → «النتيجة معتمدة؛ ألغِ الاعتماد أولاً.»; clear locked → «لا يمكن مسح نتيجة معتمدة أو مطبوعة أو مسلمة.»; unreview locked → «لا يمكن إلغاء اعتماد نتيجة مطبوعة أو مسلمة.»; print unreviewed → «لا يمكن طباعة نتيجة غير معتمدة.»; balance block → «يوجد رصيد متبقٍ على حساب المريض؛ لا يمكن الطباعة.» (button shows reason and disables); deliver unprinted → «لا يمكن تسليم نتيجة غير مطبوعة.». `AutoReviewAndComplete` auto-review is backend behaviour — no UI change. Screen opens only for `ResultKind=Simple` (routing enforced at R1). Exit: back to refreshed R1.

**Verification Gate — VG-04:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: all eight commands consumed; confirmations on clear and unreview; verbatim lock messages reachable; flag ComboBox restricted to 0–2.
3. Manual (recorded): save with empty value → verbatim validation; reviewed entry locks editing; clear behind confirmation succeeds on unlocked; balance-blocked print shows verbatim reason; auto-flag computes when flag omitted.

**Blocking conditions:** new confirmation text is created once and recorded in the register. Commit: `[S-04] Slice 3/8: Simple result entry screen + clear dialog — loop-engineering`.

### Slice 4 — Patient result sheet (R3)

**Scope:** New `PatientResultSheetViewModel` + `PatientResultSheetView`; R1→R3 (open patient) and R2→R3 wiring.

**Spec (§2.4/R3, audited):** source `GetPatientResultSheetQuery` → `PatientResultSheetDto` (`ResultSheetLineDto` lines: `TestName/TestCode/ResultValue/ResultFlag/Notes/IsReviewed/IsPrinted/FrozenRange`). Actions: «اعتماد الكل» → `MarkAllPatientResultsReviewedCommand(PatientId)` behind bulk confirmation (returns reviewed count — surfaced); per-line print → `MarkResultPrintedCommand`; deliver → `MarkResultDeliveredCommand`; PDF export → deferred to Slice 5 (affordance present, disabled). Balance block visible here too (`BalanceProbe` backend). Reprint of an already-printed report shows `BulkPrintMessages.ReprintConfirmationMessage` verbatim: «لقد تم طباعه هذا التقرير لهذا المريض من قبل هل ترغب في اعاده الطباعه». Four states: Loading / Empty (لا تحاليل للزيارة) / Error («المريض غير موجود.») / Confirmation (review-all; reprint).

**Verification Gate — VG-05:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: `GetPatientResultSheetQuery` + `MarkAllPatientResultsReviewedCommand` consumed; reprint path uses the verbatim constant text; review-all behind confirmation; PDF affordance disabled.
3. Manual (recorded): sheet lists visit lines with frozen ranges; review-all confirms then reports count; reprint prompts with the verbatim message; balance-blocked print refuses verbatim.

**Blocking conditions:** STOP-and-report on any backend shape mismatch. Commit: `[S-04] Slice 4/8: Patient result sheet — loop-engineering`.

### Slice 5 — Bulk print dialog (R5) + PDF export (R6)

**Scope:** New bulk-print dialog VM/View; `SaveFileDialog`-based export wiring on R3 (enable the Slice-4 affordance).

**Spec (§2.4/R5, §2.4/R6, audited):** R5: source `BulkPrintPreflightQuery(PatientIds)` → per-patient `RequiresReprintConfirmation/VerifiedCount/TotalCount`; execute → `ExecuteBulkPrintCommand(decisions)` with `BulkPrintDecision.ConfirmReprint` per patient; outcomes rendered from `BulkPrintOutcomes` (`Printed/Skipped/BlockedByBalance/NoVerifiedResults/PatientNotFound`); per-patient reprint confirmation shows `ReprintConfirmationMessage` verbatim; empty selection → backend «قائمة المرضى مطلوبة.» / «قائمة القرارات مطلوبة.». R6: `ExportPatientReportPdfCommand(PatientId, AbsolutePath)` via a system `SaveFileDialog` — **the save-file picker is Requires creation in Presentation** (existing `IDialogService` exposes backup pickers only; add a narrowly-scoped picker following the existing dialog-service idiom); validation verbatim: «مسار ملف PDF مطلوب.» / «مسار ملف PDF يجب أن يكون مطلقًا.» / «مسار الملف يجب أن يكون بامتداد PDF.» / «لا يمكن تصدير تقرير غير معتمد.» / «ملف التصدير موجود مسبقًا.» / «فشل تصدير تقرير PDF.».

**Verification Gate — VG-06:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: both BulkPrint commands/queries consumed; decisions carry `ConfirmReprint`; export path absolute + `.pdf` enforced pre-call; picker addition confined to Presentation dialog service.
3. Manual (recorded): preflight lists patients with verified counts; reprint patients prompt verbatim; outcomes grid shows all five outcome kinds; export to an existing path refuses verbatim; export of unverified report refuses verbatim.

**Blocking conditions:** any printer-driver dependency beyond the existing print commands is a STOP-and-report. Commit: `[S-04] Slice 5/8: Bulk print dialog + PDF export — loop-engineering`.

### Slice 6 — Profile results screens (P1–P5) + R1 routing

**Scope:** New `ProfileEntryViewModel` + `ProfileEntryView` (P1 grid), report preview (P2), amendment dialog (P3), amendments log dialog (P4), verify confirmations (P5 via `IDialogService`); enable R1→P1 routing for `SpecializedProfile` rows (settled D4).

**Spec (§3.4, audited):** P1: source `GetProfileEntryGridQuery` → `ProfileEntryGridDto` (`ProfileEntryItemDto` rows: `AnalyteName/ResultValue/Unit/Flag/IsVerified/IsPrinted/FrozenRange`); write `SaveProfileResultsCommand(PatientTestId, Comment?, Items)` — comment lands on `PatientTestId` notes. Rules (verbatim): «معرّف الزيارة غير صالح.» / «قائمة النتائج مطلوبة.» / «يجب إدخال نتيجة مادة واحدة على الأقل.» / «لا يمكن تكرار المادة التحليلية.» / «المادة التحليلية غير ضمن مكونات البروفايل.» / «قيمة النتيجة مطلوبة.» / «البروفايل غير موجود.». Save semantics (backend-verified): full replacement of unprinted drafts; printed/amended rows never deleted; per-analyte snapshot frozen via `ProfileResultReferenceRangeCapture`. Printed rows locked from direct edit («لا يمكن تعديل مادة مطبوعة إلا عبر مسار التعديل.») — lock shape in UI is executive (disable edit, offer Amend). Actions: حفظ → Save; اعتماد → `VerifyProfileResultsCommand`; إلغاء اعتماد → `UnverifyProfileResultsCommand` (locked on printed/delivered: «لا يمكن إلغاء اعتماد نتيجة مطبوعة أو مسلمة.»); طباعة → `MarkProfilePrintedCommand` (balance block verbatim); تقرير → P2; تعديل بعد الطباعة → P3. P2: `GetProfileReportQuery` → `ProfileReportDto`, reads **frozen snapshots exclusively**; print via `MarkProfilePrintedCommand`. P3: `AmendProfileResultCommand(ProfileResultItemId, ResultValue, Unit?, Flag?, Reason?)` — value mandatory («قيمة النتيجة مطلوبة.»); reason **optional** (validator imposes no NotEmpty — audit-verified) but ≤ 500 («ملاحظة التعديل طويلة جداً.»); allowed only after print («لا يمكن تعديل نتيجة البروفايل قبل الطباعة.»). P4: `GetProfileResultAmendmentsQuery` → `ProfileAmendmentDto` list; gated `PT_AUDIT_ACCESS` — button visible, denial surfaces verbatim. P5: verify/unverify confirmations (texts created, recorded). Flag domain: `ProfileResultFlag { Low=0, High=1 }` only. Entry/exit: from R1 per D4; back to R1.

**Verification Gate — VG-07:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: all five M05 commands + three queries consumed; R1 routes `SpecializedProfile` rows to P1; no `ProfileResultsAccessPolicy` referenced (M05 reuses `ResultsEntryAccessPolicy` — grep gate); amendment dialog does not force a reason.
3. Manual (recorded): grid saves draft rows; duplicate analyte refuses verbatim; printed row edit locked, Amend path works and logs; amendments dialog refuses non-audit user; balance-blocked print verbatim; report shows frozen ranges only.

**Blocking conditions:** any ambiguity in routing data is a STOP-and-report (none expected — audit-verified). Commit: `[S-04] Slice 6/8: Profile results screens + worklist routing — loop-engineering`.

### Slice 7 — Culture results screens (C1–C3) + R1 routing

**Scope:** New `CultureEntryViewModel` + `CultureEntryView` (C1), culture report (C2), verify confirmation (C3); enable R1→C1 routing for `Culture`/`IsCultureType` rows (settled D4).

**Spec (§4.4, audited):** C1: source `GetCultureEntryGridQuery` → `CultureEntryGridDto`; write `SaveCultureResultsCommand(PatientTestId, Sample?, OrganismA?, OrganismB?, OrganismC?, CultureCondition?, ColonyCount?, Sensitivities)`. Header fields with limits: العينة ≤ 100; المستعمرة A/B/C ≤ 150 each; حالة المزرعة ≤ 200; عدد المستعمرات ≤ 50 (length limits backend-verified; default FluentValidation texts where no explicit message). Sensitivity grid: rows = antibiotics attached to the culture (sourced **internally** from `CultureAntibioticAttachment` + `Antibiotic` — do NOT call `GetCultureAntibioticsQuery`); per-row ComboBox `SensitivityCategory` ∈ [0,3] (HighlyFor/ModerateFor/LowFor/ResistantFor); duplicate antibiotic → «تكرار المضاد الحيوي في نفس النتيجة.»; unattached antibiotic → «المضاد الحيوي غير مرفق بهذه المزرعة.». Display filter (backend-verified): pregnancy-flagged rows show only on pregnancy signal; children-flagged rows only for child < 12 years (`ChildAgeThresholdYears = 12`, strictly under); saved rows always shown. Pregnancy badge: when `PregnancySignal` applies, a prominent non-hidable badge is shown (UI shape executive). Locks: «لا يمكن تعديل نتيجة مزرعة معتمدة أو مطبوعة أو مسلمة.»; type guard «هذا التحليل ليس من نوع المزارع.». Actions: حفظ → Save; اعتماد → `VerifyCultureResultCommand` (fails without saved result: «لا توجد نتيجة مزرعة للاعتماد.»; auto enters+reviews); إلغاء → `UnverifyCultureResultCommand` («لا يمكن إلغاء اعتماد نتيجة مزرعة مطبوعة أو مسلمة.»); طباعة → `MarkCultureReportPrintedCommand` (balance block verbatim via `BalanceProbe`); تقرير → C2. C2: `GetCultureReportQuery` → `CultureReportDto` (full patient data + header + rows + `PrintLabIdInsteadOfPatientId` + `PrintCount`); settings-missing error «سجل الإعدادات العامة مفقود.». C3: verify/unverify confirmations (texts created, recorded). Policy: `CultureResultsAccessPolicy` codes (EDIT/REVIEW/PRINT_RESULTS) — module-own policy (audit-verified; contrast with M05). Entry/exit: from R1 per D4; back to R1.

**Verification Gate — VG-08:**
1. Build 0/0; suite green; zero-drift.
2. Grep/inspection: all four M06 commands + both queries consumed; R1 routes culture rows to C1; no `GetCultureAntibioticsQuery` consumer added; sensitivity ComboBox restricted to 0–3; pregnancy badge bound to grid flags.
3. Manual (recorded): header limits enforced; duplicate/unattached antibiotic refusals verbatim; child <12 sees children-flagged rows, ≥12 does not; pregnancy-flagged rows hidden without signal; verify without save refuses verbatim; balance-blocked print verbatim; report honours `PrintLabIdInsteadOfPatientId`.

**Blocking conditions:** any need to alter the display-filter logic backend-side is a STOP-and-report. Commit: `[S-04] Slice 7/8: Culture results screens + worklist routing — loop-engineering`.

## 5. Programme-Level Traceability (plan → slices)

| P3 plan element (audited) | Slice |
|---|---|
| M21 S1 worklist + settled entry point («المعمل» tab) | 0 |
| M21 S2 draw board + settled `ADD_EDIT_PATIENT` scope | 1 |
| M04 R1 worklist + D4 routing skeleton + navigation debt | 2 |
| M04 R2 entry + R4 clear dialog | 3 |
| M04 R3 patient sheet | 4 |
| M04 R5 bulk print + R6 PDF export | 5 |
| M05 P1–P5 + D4 routing (settled) | 6 |
| M06 C1–C3 + D4 routing (settled) | 7 |
| Three owner decisions (D4, entry point, permission scope) | **closed — binding everywhere; grep-gated in 0/1/2/6/7** |

**Explicit non-goals (scope containment):** no new backend command/query/DTO/validator; no migration; no `PermissionConfiguration` change; no new permission code; no standalone M05/M06 shell entries (D4); no «Laboratory» shell icon (RLS-inspiration-only); no Word export (Top-Lab is PDF-only); no NATIGH.COM upload (RLS-inspiration-only); no unsaved-changes confirmations beyond what the plan documents; the «الحسابات»/«الأدوات»/«الإحصائيات»/«النظام» shell gaps stay as-is (other passes).

---

## 6. Corrections Log (audit of the original P3 plan — see the memory file §"Corrections Log" for the authoritative record)

Recorded in `S-04-memory.md` (the location prior passes use for audit/verification registers). Summary: **3 corrections** — (C1) `GetPatientsWithUncollectedSamplesQuery` documented PageSize default corrected/annotated to the live signature `PageSize = 100`; (C2) plan §1.4/§1.6 "بانتظار قرار المالك" markers on the S1 entry point and the draw-permission scope replaced with the owner-approved final content (§3 above); (C3) the «حسم القرار D4» section's "قرار افتراضي من الوكيل — بانتظار موافقة المالك" marker replaced with «قرار نهائي معتمد من المالك», reasoning unchanged. No fabricated code fact was found; every "Confirmed from code" claim spot-audited held against the live commit.
